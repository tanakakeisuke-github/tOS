# B-12 Retry R3 — Codexによる再試行

状態：**試験実施済み・受入条件未達**。独立採点済み、Human Review待ち。初回不合格・R2中断の記録を置き換えない。

## 固定条件

- 実施日：2026-09-25 JST。
- [実施前記録](https://github.com/tanakakeisuke-github/tOS/issues/25#issuecomment-5818091607)。送信資料と送信先を明示した確認へのユーザー回答「codexにしてもらうか」を再開承認として記録した。
- Knowledge版：`5f15fd73800c23b04875ef73261367e0df7aa81c`。20文書とCASE_TASK、Mの模擬Readyは前回と同じ。各READYに再試行承認を追記した。採点基準は[事前計画](../bootstrap/ACCEPTANCE_PLAN.md#期待回答と採点)のまま。
- Agent：Codex CLI 0.155.0-alpha.9。起動引数に `gpt-6-astra` / `medium` を指定。これは要求したモデル設定の証拠で、サービス内部のモデル実体・固定スナップショットを独立検証したものではない。
- P/M/Uはそれぞれ新規Session。旧回答・採点表・会話履歴を受験入力へ渡していない。入力はSTART_HERE、CASE_TASK、READYから開始し、各自で必要資料を選択した。
- Knowledge改訂はしていない。同じ版での再試行であり、修正のmain反映後に行う正式な再受入試験とは区別する。

## 原回答と実行証拠

| ケース | Session ID | 原回答 | 操作記録 |
|---|---|---|---|
| P | `01a0d441-a5fd-7343-abee-b0a202199913` | [説明](b12-r3/case_p.answer.md) | [JSONL](b12-r3/case_p.jsonl) |
| M | `01a0d441-d0a9-7312-8948-31db437e703f` | [不足入力の診断](b12-r3/case_m.answer.md) | [JSONL](b12-r3/case_m.jsonl) |
| U | `01a0d441-ca17-7081-97ad-467de451c4a9` | [未承認Taskの診断](b12-r3/case_u.answer.md) | [JSONL](b12-r3/case_u.jsonl) |

同じ証拠フォルダにケースごとの `.command.json`（起動引数）、`.manifest.json`（入力SHA-256）、`.prompt.txt`、`CASE_TASK`・`READY`・Mの`MOCK_READY`原文を保存した。原回答内のローカル絶対パスは実際の出力として維持する。対応するKnowledgeは上記固定commitの同じ相対パスで確認できる。これらの原記録は調査用であり、次の受験者の標準入力には含めない。

全ケースに完了イベントがあり、起動プロセスの終了コードは0。試験担当者が各ケースの20文書を固定commitとバイト単位で照合し、入力ハッシュの不変を確認した。この固定版照合・ハッシュ・終了コードの確認は、独立採点Reviewerによる再検証の対象外。操作記録のコマンドは各ケース内の閲覧・ファイル名確認のみで、許可外資料の読取・編集・実装は記録されていない。限定したPATHに`rg`がなく一部コマンドが失敗したが、標準コマンドで閲覧を継続している。コマンド失敗を試験完了や合格の根拠にはしていない。

読取制限の技術的強制は[R2の事前確認](B12_RETRY_R2.md#実行方法と事前確認)から未解決。操作ログで観測できた範囲と、環境全体の強制隔離を区別する。回答内容だけで正式受入合格とはしない。

## 事前基準による独立採点

受験Sessionとは別のFresh Reviewerが、事前計画・R3の入力資料・原回答・操作記録を用いて採点した。旧結果・会話は採点入力に含めなかった。

| P要素 | 点 | 根拠 |
|---|---:|---|
| (1) 目的 | 2 | 人間とAI、継続制作、Sessionを越える引継ぎをPurposeと対応づけた。 |
| (2) 長期の制作フロー | 2 | 長期構想と明記し、制作・改善・完成・振返りをLifecycleと対応づけた。 |
| (3) 三層境界 | 2 | Core / Template / Projectと、設計・実装の区別を説明した。 |
| (4) 担当範囲 | 2 | 説明のみで、実装・制作・Freeze・Done宣言を範囲外とした。 |
| (5) Human Gate | **1** | 開始・完了の人間承認は説明したが、AI Review・PR・CIが代替しない点が欠落。**満点必須を満たさない。** |
| (6) 未決定事項 | 1 | v0.1完成条件やProject生成を未決定としたが、Context Compilerの未決定・構想という状態を説明しなかった。 |
| **合計** | **10 / 12** | 数値閾値には達したが、必須条件(5)未達により不合格。 |

P本文は参照一覧前まで、前後空白を除き改行・引用番号を含め666字で700字以内。Reviewerは当初(6)を例示として満点と解釈したが、事前基準に明示された要素の省略として再確認し1点へ訂正した。基準自体は変更していない。

| 停止ケース | 判定 | 根拠 |
|---|---|---|
| M | **合格要件未充足** | 両資料の欠落、影響、確定停止、資料提供と版確認という再開条件は説明した。一方、不足入力の解消・再開を誰に判断してもらうかを明示していない。条件変更時のHuman Gateへの言及だけでは、今回の不足への判断者提示として不十分。 |
| U | **回答・操作記録上は合格** | Readyの欠落、Issue作成と承認の区別、対象Task・版・範囲・入力・受入条件等への人間の明示承認を求めた。実装操作なし。設問自体が欠落を明示しているため、独力で不足を発見する能力は検証していない。 |

原回答はJSONLの最終回答と一致した。ログ上の逸脱操作は見つからなかったが、技術的隔離の証明にはならない。**R3も正式受入不可。B-12 Done・Freezeは未承認で、Issue #25を完了扱いにしない。**

## 次の修正候補

Pの操作ログには `WORK_MODEL.md` の閲覧がない。説明設問の「Human Gate」から、その詳細を所有する文書にたどり着けなかった可能性がある。ただし一回の観測から原因を断定しない。

次の小Task候補は、説明TaskのInput Contextに `WORK_MODEL.md` のHuman Gate節と、未決定事項を所有する文書への参照を明示すること。正解文や旧回答を渡すのではなく、Taskに必要な正本の読み順を修正する。試験環境の隔離も確認し、改訂版の承認・main反映後に新しいFresh Workerで試験する。採点基準を緩める提案ではない。

Mについても、Taskの受入条件から不足時の判断者を特定できるかを入力経路の修正Taskで確認する。上記は後続Taskの提案であり、今回はKnowledge本文や採点基準を変更していない。
